apiVersion: v1
kind: Service
metadata:
  name: gateway-app
  labels:
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: gateway-app
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 8080
      targetPort: http
